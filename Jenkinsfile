pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo build'
            }
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'echo test'
            }
        }
    }
}
